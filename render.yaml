services:
  - type: web
    name: emotion-analysis-app
    env: python
    preDeployCommand: ""
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.5
      - key: FLASK_ENV
        value: production